class_name: ApiTester
modules:
    enabled:
        - REST:
            url: /api/
            depends: Laravel5
        - \Helper\Api
        - Laravel5:
            environment_file: .env.testing
        - Asserts
    config:
        - Db:
            dsn: 'mysql:host=localhost;dbname=uwetranslate'
            user: 'homestead'
            password: 'secret'
            dump: tests/_data/dump.sql